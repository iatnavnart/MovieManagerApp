import React, {Component} from 'react';
import {Text, View} from 'react-native';

export class MovieDetailScreen extends Component {
  render() {
    return (
      <View>
        <Text> MovieDetail Component </Text>
      </View>
    );
  }
}

export default MovieDetailScreen;
